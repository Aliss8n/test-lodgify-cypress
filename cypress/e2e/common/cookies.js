import { defineStep as And } from "@badeball/cypress-cucumber-preprocessor";

const { map } = require("../../support/components");

And("I allow all cookies", () => {
  // TODO
  // Not implemented because this is only a test form without cookies
  // This step is a placeholder for solving CAPTCHA manually or using a service.
  // In a QA environment, you might use a mock or a test CAPTCHA that can be solved automatically.
});
